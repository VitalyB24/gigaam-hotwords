# An example dictionary for g2h.py: one term per line, spelled the way it should appear in the transcript.
# Abbreviations in capitals; a name with a capital (it is then spelled only that way); other terms in lower case
# (they also get the form with a capital first letter). Only terms that are actually said and that plain decoding gets
# wrong: not the ordinary words the model writes by itself (only the dictionary form gets the bonus, so their endings
# would change) and not terms of two letters (they get written into other words).
# Check it: python g2h.py --dict terms.example.txt --dict-check

# abbreviations
НДС
ТТН
ТН ВЭД
ГОСТ

# names
Госстандарт

# terms plain decoding spells differently
надбавка импортёра
